# Q1 结果素材（Stage 7，未进入 Stage 8）

正式基准采用瞬时纯追踪、固定烟幕中心、80 m 舰船等效探测圆盘、分段直线 UAV、2 s 响应和 3.5 s 起爆延时。4 个来袭方向 × 4 个初始距离共 16 个场景仍全部为 `certified_infeasible`；正式最佳候选的事件时刻、覆盖时长、最大连续裸露、裕度和航程未改变。

算法比较保留 multi-start SLSQP、Sobol+局部精修、SHGO、DE+SLSQP。Sobol 平均最大裸露由旧实现约 8.9100 s 降至约 8.5751 s，但 SHGO 仍为 winner。失锁图固定使用正式场景行的同一 `candidate_id`，只改变模型，反事实不进入正式排序。结论应写为当前有限预算下的最好已知方案，不写全局最优。
