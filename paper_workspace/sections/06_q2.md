# Q2 结果素材（Stage 7，未进入 Stage 8）

Q2 使用 Q1 warm start、事件锚点组合、SLSQP 连续精修和多烟幕联合覆盖 verifier。四个代表场景当前最佳多弹候选均为 `certified_infeasible`；联合覆盖下界约为 17.2634–17.2710 s，最佳单烟幕基线约为 10.3738 s，正式 `joint_gain_s` 约为 6.8896–6.8971 s。该增益是联合覆盖下界减同场景最佳单烟幕下界，不是联合覆盖下界本身。

结果同时给出 `total_exposure_lower_s`/`upper_s`、认证覆盖/裸露区间、`maximum_continuous_exposure_s` 认证下界、可能上界和 unresolved 区间。搜索是可提交的有界候选流程，不能写成完整 SIP 全局精确求解。
