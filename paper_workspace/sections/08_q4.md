# Q4 结果素材（Stage 7，未进入 Stage 8）

Q4 只消费已认证任务包，并采用不读取未来任务的因果滚动策略。在线 rolling 和 greedy 在可选过滤阶段同时要求任务已揭示、资源可用且 `certified is True`；未认证/未闭合任务不进入 `decisions`、不消耗容量，并保留在 `unresolved_task_ids`。abundant、critical、shortage 三种资源工况仍分别为 rolling=greedy=hindsight：46.4、33.0、17.2；hindsight 仅为离线上界。结果见 `results/q4_rebuild/q4_results.json`。
