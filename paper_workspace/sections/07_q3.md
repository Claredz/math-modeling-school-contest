# Q3 结果素材（Stage 7，未进入 Stage 8）

主解释为三架无人机每架恰好投放一枚。三条 UAV 路径分别通过作战半径验证，三个固定烟幕由联合 verifier 统一检查；放松的“至多一枚”不进入主结论。修复后 Q3 复用 Q2 的覆盖区间、最大连续裸露和同场景最佳单弹基线语义，四个代表场景仍保留 `certified_infeasible`。

代表性方案、起爆时刻、覆盖/裸露上下界、`joint_gain_s` 和未决区间位于 `results/q3_rebuild/q3_results.json`，图位于 `figures/q3_rebuild/`。失效或未决状态必须原样保留，不能用三机数量暗示严格全窗口必然可行。
