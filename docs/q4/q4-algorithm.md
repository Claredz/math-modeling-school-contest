# Q4 算法

在每个滚动时刻加入已揭示任务，先过滤 `certified is True` 且资源可行的任务，再按价值/资源成本进行因果滚动选择；同时运行只按价值排序的因果贪心基线，以及只在离线分析中使用的枚举 hindsight 上界。未认证、未闭合、未揭示或资源不足的任务保留为 `unresolved`，未来任务不会进入在线排序。所有三种资源工况共用同一任务包接口，并报告 rolling–greedy gap 和 hindsight gap。

完整 LBBD、通用 MINLP 和未来信息反事实不在本次快速实施范围内。
