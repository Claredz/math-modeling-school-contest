# Q3 验证

三条 UAV 路径共享相同的事件语义，但各自独立通过作战半径证书；路径两两调用 pairwise separation 证书，正式基准使用来源可支持的 0 m 安全下界，并把最小距离显式输出。联合 verifier 对三个烟幕的并集进行时间—空间细分，输出覆盖下界、上界、总裸露、最大连续裸露和未决单元。任意一条路径超出半径或冲突证书失败即为 `certified_infeasible`。

候选按成功优先的词典序排序，并记录最大连续裸露、最小路径间距等 ε 约束字段；没有来源的正安全距离不会伪造为题面事实。

结果标签明确为三机主解释；放松的“至多一枚”没有被当作主结论。
