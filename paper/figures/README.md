# 论文图片证据清单

本目录只包含当前冻结基线的既有图片副本，不在 Stage 8 重新生成科学图表。Q1 图片来源于仓库根目录的 `figures/q1_rebuild/`，Q2--Q4 与灵敏度图片分别来源于 `figures/q2_rebuild/`、`figures/q3_rebuild/`、`figures/q4_rebuild/`、`figures/sensitivity_rebuild/`；复制后的 SHA-256 在 `paper/paper_audit.md` 中记录。`model_workflow.svg` 保留为来源资产，但电子版正文只嵌入可由 XeLaTeX 直接读取的 PNG 图片。
